# Copy to .env.local for local dev (optional — defaults to http://localhost:8000).
# In production (Vercel/Netlify), set this in the host's Environment Variables to your
# Render backend URL, e.g. https://ur-tutor-api.onrender.com
VITE_API_BASE_URL=http://localhost:8000
